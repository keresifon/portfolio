version: 2.1

orbs:

  docker: circleci/docker@1.7.0
  aws-eks: circleci/aws-eks@1.1.0
  aws-cli: circleci/aws-cli@2.0.3

workflows:
  docker-build:
    jobs:
      # - docker/publish:
      #     lint-dockerfile: true
      #     docker-password: DOCKER_PASSWORD
      #     docker-username: DOCKER_USERNAME
      #     dockerfile: Dockerfile
      #     tag: latest
      #     image: keresifon/eks
      - aws
          # requires:
          #   - "docker/publish"
      
 
      

jobs:
  # kuber:
  #   docker:
  #     - image: keresifon/eks:latest
  #   steps:
  #     - checkout
 
  aws:
    docker:
      - image: keresifon/eks:latest
    steps:
      - checkout
      # - run:
      #     command: |
      #       apt-get update
      #       apt-get -qq -y install curl
      #       apt-get install -y python
      #       apt-get install -y gnupg
      #       apt-get install -y unzip
      #       curl -LO https://github.com/kubernetes/kops/releases/download/$KOPS_VERSION/kops-$PLATFORM-amd64
      - aws-cli/install:
          disable-aws-pager: true 
      - aws-cli/setup:
          aws-access-key-id: AWS_ACCESS_KEY 
          aws-region: AWS_DEFAULT_REGION 
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY 
          profile-name: eks 
      
        
  eks:
    docker:
      - image: keresifon/eks:latest
    steps:          
      - aws-eks/update-kubeconfig-with-authenticator:
          install-kubectl: true 
          cluster-name: portfolio-eks-ITFEs2sZ 
          cluster-authentication-role-arn: arn:aws:iam::870899748481:user/kereawsd 
          aws-region: us-east-1 

     
      
